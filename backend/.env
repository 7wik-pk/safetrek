# Database connection
DATABASE_URL=postgresql://postgres:pass@postgis:5432/strek

# CORS origins (comma-separated)
ALLOWED_ORIGINS=http://localhost,http://safetrek-web,http://localhost:80,*

# Optional: logging verbosity
LOG_LEVEL=info

# Optional: API versioning or prefix
API_PREFIX=/api

# Optional: secrets for auth or tokens
SECRET_KEY=strek-secret

# Optional: environment mode
ENV=development
